'use client';

const DetailsClientCmp = ({ data: dataFromServer, type }) => {
  console.log('dataFromServer: ', dataFromServer, type);
  return (
    <div>
      <div>DetailsClientCmp</div>
    </div>
  );
};

export default DetailsClientCmp;
